apiVersion: batch/v1
kind: Job
metadata:
  name: __KANIKO_JOB_NAME__
  namespace: __NAMESPACE__
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        allow-egress-internet: "true"   # ← egress 허용 라벨
    spec:
      restartPolicy: Never
      volumes:
        - name: docker-config
          secret:
            secretName: docker-config
      containers:
        - name: kaniko
          image: gcr.io/kaniko-project/executor:v1.23.0
          args:
            - --context=git://github.com/__GITHUB_OWNER__/__GITHUB_REPO__.git#__GIT_SHA__
            - --dockerfile=Dockerfile.runtime
            - --destination=ghcr.io/__GITHUB_OWNER__/__GITHUB_REPO__:runtime
            - --destination=ghcr.io/__GITHUB_OWNER__/__GITHUB_REPO__:runtime-__BUILD_KEY__
            - --cache=true
            - --cache-repo=ghcr.io/__GITHUB_OWNER__/__GITHUB_REPO__/cache
            - --verbosity=info
          env:
            - name: GODEBUG
              value: "ipv6=0"               # ← IPv6 강제 비활성화 (Go 런타임)
            - name: DOCKER_CONFIG
              value: /kaniko/.docker
          volumeMounts:
            - name: docker-config
              mountPath: /kaniko/.docker/config.json
              subPath: .dockerconfigjson
              readOnly: true

